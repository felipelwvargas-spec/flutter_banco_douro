import 'package:flutter/widgets.dart';

/// Classe que centraliza todas as cores usadas na aplicação
class AppColor {
  /// Cor laranja principal (botões, elementos destaque)
  static const Color orange = Color(0xFFFFA902);
  
  /// Cor cinza clara (fundos, AppBar)
  static const Color lightGray = Color(0xFFF0F0F0);
  
  /// Cor laranja clara (cards, cards de conta)
  static const Color lightOrange = Color(0xFFFFEDC2);
}